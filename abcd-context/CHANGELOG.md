# Changelog

## Current

- `Skill Identity` Renamed the skill from `evolve-context` to `abcd-context`. Impact: the skill name now matches the ABCd memory architecture: `AGENTS.md`, `BACKLOG.md`, `CHANGELOG.md`, and `/docs`.
- `Skill Portability` Tightened the skill description and README around project-neutral, independent context maintenance. Impact: ABCd remains a reusable memory protocol while stricter local overlays can compose without being mirrored into the skill.
