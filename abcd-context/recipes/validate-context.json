{
  "name": "validate-context",
  "description": "Run the ABCd context validator for a project root.",
  "args": ["path:path=."],
  "defaults": {
    "skill_dir": "~/.pi/agent/skills/abcd-context"
  },
  "template": "{skill_dir}/scripts/validate-context.mjs {path}"
}
